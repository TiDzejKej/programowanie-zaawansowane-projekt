@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["SuccessMessage"]
    </div>
}

<h2>Formularz kontaktowy</h2>
<form asp-action="SendEmail" asp-controller="ContactForm" method="post" class="needs-validation" novalidate>
    <div class="mb-3">
        <label for="Title" class="form-label">Tytu³</label>
        <input type="text" class="form-control" id="Title" name="Title" required>
        <div class="invalid-feedback">Proszê podaæ tytu³.</div>
    </div>
    <div class="mb-3">
        <label for="Content" class="form-label">Treœæ</label>
        <textarea class="form-control" id="Content" name="Content" rows="5" required></textarea>
        <div class="invalid-feedback">Proszê podaæ treœæ wiadomoœci.</div>
    </div>
    <div class="mb-3">
        <label for="Email" class="form-label">Email</label>
        <input type="email" class="form-control" id="Email" name="Email" required>
        <div class="invalid-feedback">Proszê podaæ poprawny adres email.</div>
    </div>
    <button type="submit" class="btn btn-primary w-100">Wyœlij</button>
</form>
